package com.tj703.employees.dto;

public class Order {
    public enum Column{
        emp_no,
        birth_date,
        first_name,
        last_name,
        gender,
        hire_date,
        dept_no,
        dept_name
    }
    public enum Direct{
        DESC,ASC
    }
}
